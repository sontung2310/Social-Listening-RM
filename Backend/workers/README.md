# Workers

Placeholder for the future SQS AI worker pool.

Will consume `crawl.ai.queue`, run NLP via `ai/`, write cloud Mongo, and route failures to the DLQ.
See `code/FUTURE_WORK.md` and `code/SYSTEM_ARCHITECTURE.md`.
